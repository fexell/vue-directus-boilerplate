# Vue/Nuxt boilerplate, with Directus (cms) for backend
This is a vue/nuxt boilerplate with Directus setup (as backend)

# Installation
```
git clone https://github.com/fexell/vue-directus-boilerplate.git <your-project-name>
```